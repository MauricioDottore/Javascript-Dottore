// a. Crear dos variables numéricas y utilizar el operador suma para guardar el valor de la suma de ambos números en una 3er variable.
let num1 = 500;
let num2 = 2000;
let sum = num1 + num2;
console.log('La suma de', num1, 'y', num2, 'es:', sum);

// b. Crear dos variables de tipo String y concatenarlas guardando el resultado en una 3er variable.
let string1 = "Hola, ";
let string2 = "World!";
let concatenatedString = string1 + string2;
console.log('Concatenación de strings:', concatenatedString);

// c. Crear dos variables de tipo String y sumar el largo de cada variable (cantidad de letras del string) guardando el resultado de la suma en una 3er variable (utilizar length).
let str1 = "JavaScript";
let str2 = "es genial";
let totalLength = str1.length + str2.length;
console.log('La suma de la longitud de los strings es:', totalLength);
